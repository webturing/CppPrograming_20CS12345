#include<bits/stdc++.h>

using namespace std;

int main() {

    int a, b;
    cin >> a >> b;
    for (int n = a; n <= b; n++) {
        if (n < 100)continue;
        if (n > 999)continue;

        if (1) {
            cout << n << endl;
        }

    }


    return 0;
}
